initscript {
    repositories {
        flatDir {
            dirs '{{PLUGINS_DIR}}'
        }
    }
    dependencies {
        classpath name: 'xgradle-core'
    }
}

gradle.apply plugin: org.altlinux.xgradle.impl.plugin.XGradlePlugin