initscript {
    repositories {
        flatDir {
            dirs '{{PLUGINS_DIR}}'
        }
    }
    dependencies {
        classpath name: 'xgradle'
    }
}

gradle.apply plugin: org.altlinux.xgradle.plugin.XGradlePlugin