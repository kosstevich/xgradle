initscript{
    repositories {
        flatDir{
            dirs(gradle.gradleHomeDir.getAbsolutePath() + "/xgradle")
        }
    }
    dependencies {
        classpath name: "xgradle-resolution-plugin"
    }
}

gradle.apply plugin: org.altlinux.xgradle.impl.plugin.XGradlePlugin
